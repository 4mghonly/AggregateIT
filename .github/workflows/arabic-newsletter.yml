name: Arabic geopolitical newsletter
on:
  push:
    branches: [Arabic]
    paths:
      - arabic_newsletter/**
      - .github/workflows/arabic-newsletter.yml
# A branch-only schedule cannot use GitHub cron. The connected six-hour
# automation updates arabic_newsletter/trigger.json on Arabic to emit a push.
permissions:
  contents: read
concurrency:
  group: arabic-newsletter-only
  cancel-in-progress: false
jobs:
  arabic:
    if: github.ref == 'refs/heads/Arabic'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      ARABIC_STATE_DIR: arabic_newsletter/runtime/state
      ARABIC_LLM_API_KEY: ${{ secrets.ARABIC_LLM_API_KEY || secrets.QWEN_API_KEY }}
      ARABIC_LLM_BASE_URL: ${{ secrets.ARABIC_LLM_BASE_URL || secrets.QWEN_BASE_URL }}
      ARABIC_LLM_MODEL: ${{ vars.ARABIC_LLM_MODEL || vars.QWEN_MODEL || 'qwen3.8-flash' }}
      DISCORD_WEBHOOK_ARABIC: ${{ secrets.DISCORD_WEBHOOK_ARABIC }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: arabic_newsletter/requirements.txt
      - name: Install isolated dependencies and Arabic fonts
        run: |
          python -m pip install -r arabic_newsletter/requirements.txt
          sudo apt-get update -qq
          sudo apt-get install -y fonts-dejavu-core fonts-noto-core libraqm0
      - name: Restore Arabic state only
        id: restore
        uses: actions/cache/restore@v4
        with:
          path: arabic_newsletter/runtime/state
          key: arabic-state-v1-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: arabic-state-v1-
      - name: Test Arabic pipeline
        if: "!startsWith(github.event.head_commit.message, 'Arabic newsletter: scheduled')"
        run: python -m unittest arabic_newsletter.test_arabic -v
      - name: Render long Arabic sample
        if: "!startsWith(github.event.head_commit.message, 'Arabic newsletter: scheduled')"
        run: python -m arabic_newsletter.run --sample --long --output arabic_newsletter/runtime/sample
      - name: Validate Arabic model configuration
        run: python -m arabic_newsletter.run --preflight --probe-model
      - name: Audit sources from runner
        if: "!startsWith(github.event.head_commit.message, 'Arabic newsletter: scheduled')"
        run: python -m arabic_newsletter.run --audit --output arabic_newsletter/runtime/audit
      - name: Render current-data preview without Discord delivery
        if: "startsWith(github.event.head_commit.message, 'Arabic preview:')"
        run: |
          END="$(date --iso-8601=seconds)"
          python -m arabic_newsletter.run --end "$END" --output arabic_newsletter/runtime/preview
      - name: Render and push approved Arabic layout sample
        if: "startsWith(github.event.head_commit.message, 'Arabic sample:')"
        run: |
          python -m arabic_newsletter.run --sample --output arabic_newsletter/runtime/discord-sample
          python - <<'PY'
          import json, os
          from contextlib import ExitStack
          from pathlib import Path
          import requests

          webhook=os.environ.get("DISCORD_WEBHOOK_ARABIC","").strip()
          if not webhook:
              raise SystemExit("DISCORD_WEBHOOK_ARABIC missing")
          paths=[
              Path("arabic_newsletter/runtime/discord-sample/arabic-p1.png"),
              Path("arabic_newsletter/runtime/discord-sample/arabic-p2.png"),
              Path("arabic_newsletter/runtime/discord-sample/arabic-p3.png"),
          ]
          with ExitStack() as stack:
              files={f"files[{i}]":(p.name,stack.enter_context(p.open("rb")),"image/png") for i,p in enumerate(paths)}
              payload={"content":"نموذج تصميم تجريبي للموجز العربي — ليس أخباراً حقيقية","allowed_mentions":{"parse":[]}}
              r=requests.post(webhook,params={"wait":"true"},data={"payload_json":json.dumps(payload,ensure_ascii=False)},files=files,timeout=(10,60))
          r.raise_for_status()
          print("Arabic layout sample delivered; message ID:",r.json().get("id"))
          PY
      - name: Collect, translate, verify and render live slides
        if: "!startsWith(github.event.head_commit.message, 'Arabic preview:') && !startsWith(github.event.head_commit.message, 'Arabic sample:')"
        run: |
          if [ -n "$DISCORD_WEBHOOK_ARABIC" ]; then
            python -m arabic_newsletter.run --send
          else
            python -m arabic_newsletter.run
            echo "::warning::Live slides rendered but delivery requires DISCORD_WEBHOOK_ARABIC."
          fi
      - name: Preserve Arabic delivery ledger even after failures
        if: always()
        uses: actions/cache/save@v4
        with:
          path: arabic_newsletter/runtime/state
          key: arabic-state-v1-${{ github.run_id }}-${{ github.run_attempt }}
      - name: Upload Arabic samples, sources and run reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arabic-newsletter-${{ github.run_id }}
          path: |
            arabic_newsletter/runtime/output/
            arabic_newsletter/runtime/sample/
            arabic_newsletter/runtime/audit/
            arabic_newsletter/runtime/preview/
          retention-days: 7
          if-no-files-found: warn
